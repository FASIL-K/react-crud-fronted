import { createSlice } from "@reduxjs/toolkit";

const initialState = {
    userinfo:{},
}

const DeleteSlice = createSlice({

    name:"user",
    initialState, 
    reducers:{
        DleteEmail:(state,action)=>{
            
        }
    }


})